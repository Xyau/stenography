package bytes;

public class CompressedBytes {
    byte[] bytes;

    public CompressedBytes(byte[] bytes) {
        this.bytes = bytes;
    }

    public byte[] getBytes() {
        return bytes;
    }
}
